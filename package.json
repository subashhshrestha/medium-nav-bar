{
  "name": "starter-template",
  "version": "1.0.0",
  "description": "This is html starter template with sass",
  "main": "app.js",
  "scripts": {
    "scss":"node-sass --watch scss -o css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "subash",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.0"
  }
}
